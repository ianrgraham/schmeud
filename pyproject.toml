[project]
name = "schmeud"
version = "0.2.0"
dependencies = [
    "freud-analysis>=2.7.0"
]

[virtualenvs]
create = true
in-project = true

[build-system]
requires = ["maturin>=0.12,<0.13"]
build-backend = "maturin"